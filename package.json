{
  "name": "always-color-text",
  "version": "1.13.0",
  "private": true,
  "scripts": {
    "build.js": "node build.js",
    "build": "node build.js",
    "test": "vitest --run"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "fast-check": "^4.6.0",
    "vitest": "^4.1.4"
  }
}
