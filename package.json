{
  "name": "always-color-text",
  "version": "1.16.2",
  "private": true,
  "scripts": {
    "build.js": "node build.js",
    "build": "node build.js",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@esbuild/linux-x64": "^0.28.2",
    "esbuild": "^0.27.2",
    "fast-check": "^4.6.0",
    "vitest": "^4.1.4"
  }
}
